name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Docker Semantic Version
      uses: paulhatch/semantic-version@v4.0.3
      with:
        major_pattern: "major:"
        # Same as above except indicating a minor change, supports regular expressions wrapped with '/'
        minor_pattern: "feat:"
        # A string to determine the format of the version output
        format: "${major}.${minor}.${patch}-prerelease${increment}"
        bump_each_commit: true
      id: version
    
    - name: Docker login
      env:
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        NEW_VERSION: ${{ steps.version.outputs.version }}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
        echo "New_verision: $NEW_VERSION!!!!!"
    
    # - name: Build the Docker image
    #   run: |
    #     docker build -t mlopezperfect/docker-graphql:0.0.2 .
    #     docker build -t mlopezperfect/docker-graphql:latest .
        
    # - name: Push the Docker image
    #   run: |
    #     docker push mlopezperfect/docker-graphql:0.0.2
    #     docker push mlopezperfect/docker-graphql:latest
    # - name: Build the Docker image
    #   run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
